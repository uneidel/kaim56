# Defaults baked into the rootfs (/app/config.env). The manager's config disk
# (/config/config.env) is sourced AFTER this file and overrides every key —
# that is where the instance's template params and the platform values land.
TRANSPORT=web
GREETING=skeleton heard:
WEB_PORT=8080
