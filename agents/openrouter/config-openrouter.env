# Baked defaults (the instance config disk overrides these).
SIGNAL_API=https://signal-api.example.com
SIGNAL_VERIFY=true
CLAUDE_WORKDIR=/home/node/workspace
AGENT_NFS=1
AGENT_EXPORT=/
POLL_TIMEOUT=10
AGENT_MAX_STEPS=12
BASH_TIMEOUT=120
